{% extends 'AppBundle:user:index.html.twig' %}

{% block title %}
   ARTICLEs
{% endblock %}



{% block body %}
<h2>Articles list</h2>
    <hr>

    {% for article in articles %}
        <h3><a href="{{ path('admin_article_edit', {id: article.id}) }}">{{ article.title }}</a></h3>
        {#{{ article.content }}#}

    {% endfor %}

{% endblock %}

